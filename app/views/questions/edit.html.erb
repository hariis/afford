<h1>Editing question</h1>

<% form_for(@question,  :url => {:action => :update, :id => @question.id }) do |f| %>
  <%= f.error_messages :header_message => "Invalid Data!", :message => "", :header_tag => :h3 %>

  <%= render 'personal_item_field', :f => f %>

  <%= f.submit 'Update' %>
  
<% end %>
